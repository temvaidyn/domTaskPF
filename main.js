
function roundUp(x) {
    return Math.ceil(x);
}

function CalculateMagic(a, b) {
    // add your code here
    
    
    // remember to call roundUp() with your result before returning
    // return roundUp(g);
}